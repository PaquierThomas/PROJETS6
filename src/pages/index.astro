---
import "../styles/global.css";
---

<script>
  // Fonction pour vérifier si l'utilisateur est connecté
  async function checkUserAuthentication() {
    try {
      console.log("Vérification de l'authentification de l'utilisateur...");
      const response = await fetch("http://localhost:4000/api/verify-login", {
        credentials: "include",
      });
      console.log("Réponse reçue :", response);
      const data = await response.json();
      console.log("Données reçues :", data);

      if (response.ok) {
        console.log("L'utilisateur est connecté !");
        // Afficher le lien profil et masquer le lien connexion
        document.getElementById("connexionLink").style.display = "none";
        document.getElementById("profilLink").style.display = "block";
      } else {
        console.error("L'utilisateur n'est pas authentifié :", data.message);
      }
    } catch (error) {
      console.error(
        "Erreur lors de la vérification de l'authentification :",
        error
      );
    }
  }

  // Appeler la fonction pour vérifier l'authentification de l'utilisateur
  checkUserAuthentication();
</script>

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>An Enshrouded encounter</title>
    <link rel="icon" href="/icons/Icon.jpg" type="image/svg+xml" />
  </head>
  <html>
    <!-- Nav -->
    <div class="centered">
      <!-- Nav -->
      <nav>
        <div>
          <a id="logo" href="/"
            ><img src="/icons/logo.svg" alt="logo" /><h2>
              An Enshrouded Encounter
            </h2></a
          >
        </div>
        <ul>
          <li id="blanco"><a href="/story">Story</a></li>
          <li><a href="/characters">Characters</a></li>
          <li><a href="/statistiques">Statistiques</a></li>
          <li id="connexionLink" class="isolated">
            <a href="/connexion">Connexion</a>
          </li>
          <li id="profilLink" class="profil" style="display:none;">
            <a href="/utilisateur">Profil</a>
          </li>
        </ul>
      </nav>
      <!-- Main -->
      <main>
        <!-- <div class="intro">
          <h1>Artificial <span>Intelligence</span></h1>
          <p>
            In an interactive story about otherness, every choice matters. Explore
            diversity through an immersive narrative where your decisions shape
            the destinies of the characters.
          </p>
          <button>Start my adventure</button>
        </div> -->
        <div class="hero">
          <h1>EXPLORE CHOSE PROGRESS</h1>
          <p>
            Every choice is crucial in this interactive story about otherness.
            Your decisions shape the destinies of the characters. Dare to
            challenge norms and prepare for constant change.
          </p>

          <div class="row">
            <button><a href="/story">START MY JOURNEY</a></button><button
              ><a href="mailto:thomas.paquier.pro@gmail.com">CONTACT US</a
              ></button
            >
          </div>
        </div>
      </main>
    </div>
    <video src="/homepagevideo.mp4" autoplay muted loop></video>
    <div class="overlay"></div>
  </html>

  <style scoped>
    #logo {
      display: flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
      color: white;
    }
    /* Importation de la police Bebas Neue */
    @import url("https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap");
    .row {
      display: flex;
      gap: 20px;
    }

    .hero {
      position: absolute;
      top: 20%;
      z-index: 1;
      display: flex;
      flex-direction: column;
      /* justify-content: center; */

      font-family: "Bebas Neue", sans-serif;
      color: white;

      width: 100%;
      padding-left: 50px;

      h1 {
        font-size: 220px;
        line-height: 0.85;
        font-weight: bold;
        width: 600px;
      }
      p {
        font-size: 20px;
        font-weight: 500;
        /* margin-top: 20px; */
        font-family: "open sans", sans-serif;
        color: rgba(255, 255, 255, 0.9);

        width: 660px;
      }
      button {
        margin-top: 30px;

        background: #b53926;
        border: none;
        width: fit-content;

        padding: 10px 15px;
        padding-bottom: 5px;

        font-family: "Bebas Neue", sans-serif;
        font-size: 35px;
        transition: all 0.2s ease-in-out;
      }

      button:hover {
        cursor: pointer;
        background: #a23322;
        a {
          color: white;
        }
      }

      button:nth-child(2) {
        background: #1b1b1b;
        color: rgba(255, 255, 255, 0.7);
        border: #4c4946 2px solid;
        background-image: url("/images/noise.webp");
      }
      button:nth-child(2):hover {
        color: white;
        background: #4c4946;
      }
      a {
        text-decoration: none;
        color: rgba(255, 255, 255, 0.85);
      }
      a:nth-child(2) {
        color: rgba(255, 255, 255, 0.7);
      }
    }

    .profil {
      background: #1b1b1b;
      color: rgba(255, 255, 255, 0.7);
      border: #4c4946 2px solid;
      padding: 0 10px;
      /* padding-bottom: -10px; */
      height: 40px;
      background-image: url("/images/noise.webp");
    }
    .profil:hover {
      color: white;
      background: #4c4946;
    }
    .centered {
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: center;
      height: 100vh;
      width: 50vw;
      margin: 0 auto;
      position: relative;
    }

    video {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: -1;
    }

    main {
      position: relative;
      width: 100%;
      height: 100%;
      display: flex;
      justify-content: center;
    }

    .overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
    }

    .overlay::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: #111221e6;
    }
    nav {
      position: absolute;
      display: flex;
      left: 0;
      right: 0;
      justify-content: space-between;
      align-items: center;
      padding: 30px 40px;
      z-index: 10;
      color: #ffffff;
      font-family: "Bebas Neue", sans-serif; /* Ajout de la police Bebas Neue */
      font-size: 25px; /* Modification de la taille de la police */
    }

    nav img {
      height: 40px;
    }

    nav ul {
      display: flex;
      list-style-type: none;
      margin: 0;
      padding: 0;
      padding-right: 30px;
      gap: 40px;
    }

    nav ul li {
      margin-left: 20px;
    }

    nav ul li a,
    nav div a {
      color: rgba(255, 255, 255, 0.8);
      text-decoration: none;
      transition: all 0.2s ease-in-out;
    }

    nav div a {
      color: white;
    }

    nav ul li a:hover {
      color: white;
    }
    .isolated {
      color: rgba(255, 255, 255, 0.85) !important;
      background-color: #b53926;
      padding: 3px 14px;
      transition: all 0.2s ease-in-out;
    }
    .isolated:hover {
      color: white !important;
    }

    /* .intro {
      display: flex;
      flex-direction: column;

      position: absolute;
      top: 27%;
      left: 7%;

      width: 40%;

      h1 {
        font-size: 60px;
        font-weight: 700;
        color: #15164f;
        margin-bottom: 20px;
        line-height: 0.8;
        text-transform: uppercase;
        font-family: var(--font-black);

        span {
          font-size: 45px;
        }
      }
      p {
        font-size: 18px;
        font-weight: 500;
        color: white;
        
        width: calc(100% - 200px);
        font-family: "Montserrat", sans-serif;

        margin: 20px 0;
      }
      button {
        background: linear-gradient(75deg, #07082c 10%, #191b5a 90%);
        color: white;
        border: none;
        margin-top: 10px;

        font-weight: 700;
        font-size: 14px;
        text-transform: uppercase;
        border-radius: 25px;
        width: 220px;
        padding: 10px 0;

        cursor: pointer;
      }
    } */
    html,
    body {
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100%;
      position: relative;
      overflow: hidden;
    }

    @media (min-width: 1400px) {
      .centered {
        width: 70vw;
      }
      .hero {
        top: 22%;
      }
      .hero h1 {
        font-size: 140px;
      }
      .hero p {
        font-size: 18px;
        width: 550px;
        margin-bottom: 0;
      }
      .hero button {
        font-size: 25px;
        margin-top: 20px;
      }
    }
  </style>
</html>
